Interface funcional: Interface que implementão apenas um método. Comparator,Consumer.
Tudo feito em uma strem não afeta a lista original.
Stream evita o problema de concorrência
Stream e collection não são a mesma coisa.
Optional ser para trabalharmos de maneira mais simples com verificação de null.
TreeSet<>: Necessita da implementação do Comparable.

JDK: Kit de desenvolvimento Java -> JDK
JRE:Java Runtime Environment -> JVM: Quando você cria um programa ele roda em cima do JVM.

código font (Programa.java) -> bytecode(program.class) -> execuável
programa java só é considerado programa é necessário a função main

protected : São acessíveis apenas dentro do pacote, ou visiveis em classes herdadas.

Restições para nomes de variáveis
- Não podemos começar com digito: use _
- Não usar acentos
- Não pode ter espaço em branco
- Use nomes que tenham significado

CamelCase
- pacotes
- atributos
- metodos
- variaveis e parametros

PascalCase
- Classes